#include <bits/stdc++.h>
using namespace std;
int main(){
    string s="DataStructure";
    stack<char> st;
    for(char c:s) st.push(c);
    string r;
    while(!st.empty()){ r+=st.top(); st.pop(); }
    cout<<r<<"\n";
}
/*erutcurtSataD
*/
